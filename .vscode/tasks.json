{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "yarn dev",
            "type": "shell",
            "command": "yarn dev",  // This runs the dev server
            "problemMatcher": [],
            "isBackground": true,   // Keeps the task running in the background
            "options": {
              "cwd": "${workspaceFolder}"  // Set to your project root
            }
    ]
}
